# QReactive

Qreactive is a qr code create website. This project consists of a backend server and a frontend server. The backend server is responsible for handling API requests and database operations, while the frontend server is a Next.js application for the user interface.

Run backend server(localhost:5000):  
$npm start  
Run frontend server (localhost:3000):  
$npm run dev

